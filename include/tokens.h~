#include "stdafx.h"

typedef std::vector<std::string> Args;

class Tokens
{
     std::vector<std::string> data;
public:
     std::string push(std::string str);
     std::string function();
     Args args();
};

